package main

import (
	"fmt"
	"time"
)


/*
Just like Stringer interface, Go has an error interface
type error interface {
	Error() string
}
that fmt looks for to format  errors. 
So, if this interface is implicitly implemented, then it is called to format the error on the type it is implemented for
*/
func main()  {
	
}